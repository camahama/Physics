# Thermodynamics

Interactive thermodynamics modules for browser-based teaching and self-study.

The app follows the same Vite/TypeScript structure as `apps/electricity`:

- hash-routed modules are registered in `src/config/modules.ts`
- bilingual copy lives in `src/i18n/locales`
- each illustration module owns its own folder under `src/modules`
